/*Write a program that reads the two matrics of order mXn and pXq using function readMatrix. The program should contain a function processMatrix that takes the matrices and multiply them. The result of multiplication must be displayed using showMatrix(). Read the value of m,n,p and q from keyboard*/

#include <stdio.h>
#include <conio.h>
#include <stdlib.h>

int readMatrix(int r, int c)
{
}
int processMatrix(){

}
int displayMartrix(){
    
}
int main()
{
    const int m, n, p, q;
    system("cls");

    return 0;
}