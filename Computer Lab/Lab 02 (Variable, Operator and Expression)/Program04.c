/*Type, Compile, run and observe and think about the output of the following program*/
#include <stdio.h>
#include <conio.h>
#include <stdlib.h>

void main()
{
    int x, y, z;

    system("cls");

    x = 30000, y = 20000;
    z = x + y;

    printf("Sum = %d", z);

    printf("Press any key to exit...");

    getch();
}